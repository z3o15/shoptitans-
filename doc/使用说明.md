# 自动化装备处理系统

## 功能概述

本系统实现从游戏截图到最终表格的完全自动化处理：
1. **环境检查** - 验证依赖和配置
2. **截图裁剪** - 从游戏截图提取装备图标
3. **装备匹配** - 使用LAB色彩空间匹配装备
4. **OCR识别** - 识别金额信息生成表格

## 使用方法

### 方法一：双击运行（推荐）
直接双击 `启动自动化处理.bat` 文件

### 方法二：命令行运行
```bash
# 完整流程
python auto_equipment_processor.py

# 指定步骤
python auto_equipment_processor.py --step 1  # 环境检查
python auto_equipment_processor.py --step 2  # 截图裁剪
python auto_equipment_processor.py --step 3  # 装备匹配
python auto_equipment_processor.py --step 4  # OCR识别

# 清理后运行
python auto_equipment_processor.py --clean
```

## 目录要求

确保以下目录存在：
```
images/
├── game_screenshots/     # 游戏截图文件
└── base_equipment/      # 基准装备图片
```

## 输出结果

处理完成后在 `output/ocr/` 目录生成：
- CSV表格文件
- 处理报告
- 匹配结果

## 依赖要求

- Python 3.7+
- opencv-python
- pillow
- numpy

安装依赖：
```bash
pip install opencv-python pillow numpy
```

---

一键启动，自动处理，生成结果表格！